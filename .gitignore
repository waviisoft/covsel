node_modules/
dist/
coverage/
.covsel/
*.tsbuildinfo
.DS_Store

# Throwaway checkouts an agent works in; scoped so the team-scoped documents
# under .claude/ stay tracked.
.claude/worktrees/

# Clones the benchmark harness works in. Its results are written to
# benchmarks/results/, which is deliberately not ignored: once results exist,
# tracking them is what makes a change in the numbers show up in review.
benchmarks/.work/

# VitePress
docs/.vitepress/cache/
docs/.vitepress/dist/
