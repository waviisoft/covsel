{
  "name": "fastify",
  "repo": "fastify/fastify",
  "ref": "b912c24f422adb59f6a165fee7bf78857762f079",
  "adapter": "@covsel/adapter-generic",
  "adapterName": "generic",
  "install": ["npm", "install", "--no-audit", "--no-fund"],
  "runner": ["node", "--test"],
  "covsel": {
    "testGlobs": ["test/**/*.test.js", "test/**/*.test.mjs"],
    "sourceGlobs": ["lib/**/*.js", "build/**/*.js", "./fastify.js"]
  },
  "rationale": "A plugin architecture that wires itself together at run time, which is exactly what a static import graph cannot see: registering a plugin is a function call, not an import. Large enough that the whole suite takes real time, so it carries the speed case as well as the precision one."
}
